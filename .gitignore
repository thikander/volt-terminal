.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Updater signing keypair — never commit; the public half is baked into
# tauri.conf.json instead, so this whole dir is just a local artifact.
/.tauri

# Editor/tool-local state, not part of the project.
.vscode
.claude
